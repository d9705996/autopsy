apiVersion: v1
kind: Service
metadata:
  name: {{ include "autopsy.fullname" . }}
  labels:
    {{- include "autopsy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autopsy.selectorLabels" . | nindent 4 }}
